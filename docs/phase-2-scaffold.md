# Phase 2 Scaffold Notes

Status: complete.

Generated by:

```powershell
agents-cli scaffold create studyops-agent --agent adk --prototype --deployment-target none --agent-guidance-filename AGENTS.md --auto-approve
```

Important environment notes:

- Global `agents-cli` and `uv` were not available on PATH.
- A local workspace `.venv` was created.
- `google-agents-cli` was installed into `.venv`.
- Windows long-path limitations prevented full dependency installation through
  the normal route, so the CLI package was installed separately after the core
  dependencies landed.
- `uv.exe` and `uvx.exe` were copied into the Codex temp bin directory that is
  already on PATH for this thread.
- `agents-cli-local.cmd` wraps the local CLI for this workspace.
- Cookiecutter was configured to write replay/cache files under `work/` instead
  of the home directory.

Generated project facts:

- Project root: `studyops-agent`
- Project name: `studyops-agent`
- Deployment target: `none`
- Agent directory: `app`
- Region: `us-east1`
- Agent guidance file: `AGENTS.md`

